const add = (x, y) => {
  return x + y;
};

const PI = 3.14159;

const square = (x) => x * x;

const math = {
  add: add,
  square: square,
  PI: PI,
};

module.exports = math;

// module.exports.add = add;
// module.exports.PI = PI;
// module.exports.square = square;
